# UBE v2.3l — Tight Animation Toolbar

The animation transport now uses true fixed toolbar widths rather than native
dialog-button size hints. **Play**, **Reset pose**, **Export GLB**, **Full**, and
the Speed selector use reduced horizontal padding, and the row spacing is
tighter. The growing time/frame label no longer contributes to the main
window minimum width, so long frame counts cannot resize the application.
